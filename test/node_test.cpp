#include <fmt/core.h>
#include <graph/Node.hpp>

#include <gtest/gtest.h>

// using graph::Node;

TEST(GridGraphTest, NodeZScoreTest)
{
    // Node first{0, 0};
    // Node second{1, 0};

    // EXPECT_TRUE(first < second);
    // EXPECT_FALSE(second < first);

    // first = {1, 5};
    // second = {0, 1};
    // fmt::print("z: {}\n", first.zScore());
    // EXPECT_TRUE(first < second);
    // EXPECT_FALSE(second < first);
}
